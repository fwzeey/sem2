#include <stdio.h>
int main()
{
    int num, sum=0,digit=0;
    prrintf("Enter a number:");
    scanf("%d",num);
    if(num%2!=0);
    {
        while (num>0)
        {
            digit=num%0;
            sum=sum+digit;
            num=num/10;
        }
        } 
        
    }